<section>
    {{#if tasks}}
    {{#each tasks as |task|}}
    {{> task-card}}
    {{/each}}
    {{else}}
    <h3>No posts yet... :( Visit <a href="/profile">your profile</a> to create a new one!</h3>
    {{/if}}
</section>